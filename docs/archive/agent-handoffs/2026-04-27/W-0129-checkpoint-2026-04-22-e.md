# W-0129 — Checkpoint 2026-04-22 (E)

## Status

`CHECKPOINT` — 2026-04-22

## main SHA

`0bc77e94` (PR #173 머지 직후)

## 완료된 작업

### PR #167 — claude/silly-gagarin (W-0120/W-0123/W-0124 + Mobile)
- **W-0124**: `ChartBoard.svelte` — `subscribeClick` 클릭 핸들러, ±2bar 이내 가장 가까운 캡처 annotation 선택
- **W-0124**: `CaptureAnnotationLayer.svelte` — `onAnnotationsChange` prop 추가 (캐시 싱크)
- **W-0123**: `CaptureReviewDrawer.svelte` — `variant: 'drawer' | 'sheet'` prop + bottom sheet CSS (72vh, 16px radius, safe-area-inset-bottom, drag handle)
- **Mobile**: `ChartMode.svelte` — `CaptureAnnotationLayer` + `CaptureReviewDrawer variant="sheet"` 연결, `onChartReady + getMainSeries()` 패턴

### PR #173 — claude/w-0126-ledgerstore-supabase
- **W-0126**: `SupabaseLedgerRecordStore` — `compute_family_stats()` O(N files) → O(1) Supabase 쿼리; `get_ledger_record_store()` env 기반 자동 라우팅
- **W-0121**: `CaptureStore.save()` + `update_status()` — Supabase fire-and-forget mirror (daemon thread, non-fatal)
- **W-0122**: Phase 1A+2C+3A — engine proxy allowlist, capture contract, shared scan core; batch outcome fetch (N→1 roundtrip)
- **W-0125**: `X-Process-Time` response header + slow-request warn (>2s) + app ingress logging
- **fix**: `EXCHANGE_ENCRYPTION_KEY` check 빌드타임 → 콜타임으로 이동

### Supabase
- `capture_records` 테이블 생성 (migration 0013 실행 완료)
- 102 rows 확인 (auto_capture 정상 동작)

### Engine
- `cogotchi-00181` 배포 완료 (100% traffic), APP_ORIGIN + route collision fix 포함
- `/captures/chart-annotations` 200 OK 확인

## 미완료 (다음 세션)

| 작업 | 브랜치 | 메모 |
|---|---|---|
| PR #172 머지 | claude/gifted-raman | W-0123 indicator viz v2 — `gh auth login` 후 머지 |
| Supabase migration 018 실행 | — | `pattern_ledger_records` 테이블 (W-0126) |
| Tablet PeekDrawer | 신규 브랜치 | `CenterPanel.svelte` → selectedCapture → PeekDrawer review slot |
| W-0122 Confluence Phase 2 | 신규 브랜치 | engine-side scoring + flywheel weight learning |
