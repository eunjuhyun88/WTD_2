# Experiment report — rq-b-real-data-2026-04-11

**RQ-B real-data ladder** — first run of the harness-engine integration E5 DB read pipeline. This report is the canonical "pipeline executed, data was missing" outcome described in the pre-registration.

## Data mode

- **Mode**: `no_db`
- **Trajectories loaded**: 0
- **Source description**: (no DB connection — pipeline executed only)

## Pre-registration

- **Hypothesis bound**: first-significant-N ∈ [50, 800]
- **Schedule**: `geometric(50..800x2)`
- **Symbol filter**: BTCUSDT
- **DB fetch limit**: 10000
- **Experiment seed**: 2026
- **Bootstrap seed**: 7
- **Objective doc**: `scripts/research/experiments/rq-b-real-data-2026-04-11/objective.md`

## Hypothesis confrontation

**Result**: `INSUFFICIENT_DATA`

`DATABASE_URL` is not set in the current environment. The experiment pipeline executed end-to-end through the source acquisition step and aborted before any agent ran. This is the expected behaviour for CI / local runs without a Postgres connection.

## Acceptance checks

- Source acquisition path executed: PASS
- DatasetSource boundary contract honoured: PASS
- Insufficient-data branch handled gracefully: PASS (mode=no_db)
- Process exits 0 with a generated report: PASS

## Re-running with real data

Once the E7 live scan loop lands and the production database has at least 50 v2 decision trajectories matching the symbol filter, re-run:

```bash
DATABASE_URL=postgres://... npm run research:rq-b-real-data
```

The same script will switch from `INSUFFICIENT_DATA` mode to a full ladder run and emit per-cell CI95 statistics in this same file.

Generated by `scripts/research/experiments/rq-b-real-data-2026-04-11/experiment.mjs`.
