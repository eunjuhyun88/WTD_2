"""research.discovery — pattern discovery and research loop orchestration."""
# Minimal __init__ to avoid cascade import failures.
# Import sub-modules directly: from research.discovery.orchestrator import run_cycle
