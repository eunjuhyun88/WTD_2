# Evals

Store stable evaluation protocols, datasets, and scoring conventions here.

## Current protocol

- `research/evals/rq-b-baseline-protocol.md`

Protocol files should define the fixed baselines, metrics, schedule, and decision rule so experiment notes can reference them instead of repeating them ad hoc.
