---
description: 빠른 검증 — pytest + 품질 grep만 (≤3파일, 알고리즘 변경 없음)
argument-hint: [선택: 테스트 파일 경로]
model: claude-haiku-4-5-20251001
---

```bash
./tools/verify.py --quick $ARGUMENTS
```

BLOCK 시 `/검증` (전체)으로 전환.
