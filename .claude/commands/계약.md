---
description: 도메인 현재 계약/불변식 기록 (mutable, drift 차단용)
argument-hint: <도메인 이름>
---

도메인: $ARGUMENTS

해당 도메인의 코드를 직접 읽고 (Read/Grep) 다음 구조를 자동 추출하세요:

```
도메인: $ARGUMENTS
경로: <메인 디렉토리/파일>

공개 API:
  - <함수/라우트/컴포넌트>: <시그니처>
    설명: <한 줄>
  - ...

불변식:
  - <코드/타입/런타임에서 보장되는 규칙>
  - ...

금지 사항:
  - forbidden_props: [...]
  - forbidden_imports: [...]
  - forbidden_patterns: [...]

검증 명령:
  - `bash tools/verify_<도메인>.sh`
  또는
  - `pytest engine/tests/test_<도메인>.py -k contract`
  또는
  - `cd app && pnpm test -- <도메인>.contract.test.ts`

관련 결정:
  - memory/decisions/dec-...md
```

**필수 규칙**:
- 검증 명령은 **반드시 실행 가능한 명령** (path 포함). "코드 리뷰로 확인" 금지
- 검증 명령이 없으면 "검증 미구현 — Phase 3에서 추가 예정" 명시
- API 시그니처는 실제 코드에서 추출 (추측 금지) — 못 찾으면 명령 거부하고 사용자에게 도메인 정확한 경로 요청

사용자 [y/수정/취소] 확인 후 저장:
- 경로: `design/current/contracts/{도메인}.md`
- 디렉토리 없으면 `mkdir -p design/current/contracts`
- frontmatter: `domain`, `last_verified` (KST), `agent`

저장 후 보고: 파일 경로 + 검증 명령 (사용자가 바로 실행해볼 수 있게).
