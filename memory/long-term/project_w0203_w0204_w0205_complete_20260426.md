---
name: W-0203/W-0204/W-0205 완료 (2026-04-26)
description: active_variant_store 자동 연동 + pattern seed 3-layer 스코어링 + PromotionReport Gate UI. PR #292 rebase 머지. main=cdefda4d.
type: project
---

## 완료 항목

**W-0204** (`engine/api/routes/captures.py`):
- `POST /{capture_id}/benchmark_search`에서 `run_pattern_benchmark_search()` 호출 시 `active_variant_store=None` → `ACTIVE_PATTERN_VARIANT_STORE` 연결
- **Why:** benchmark winner가 gate 통과해도 레지스트리 등록이 항상 skip됐음 (None 조건 분기)
- **Effect:** 프로모션 후 `scan_all_patterns_live()` / `search_pattern_state_similarity()` 자동 반영

**W-0203** (`app/src/routes/api/terminal/pattern-seed/match/+server.ts`):
- 550줄 인라인 중복 로직 제거 → `runPatternSeedMatch()` 단순 위임
- dead code `return json(payload)` (payload 미정의) 제거
- **Effect:** 3-layer 스코어링, 병렬 similar-live + /search/similar, 후보 dedup 활성화

**W-0205** (PatternSeedScoutPanel):
- `PromotionGateResult` 타입을 `$lib/contracts/search/seedSearch.ts`에 정의 (브라우저 안전)
- `runPatternSeedMatch()` 반환에 `promotionGate` 필드 추가 (artifact.promotion_report 추출)
- PatternSeedScoutPanel에 PROMOTE/REJECT Gate 카드 렌더링 (decision, path, 메트릭, 게이트칩, 거절사유)

## CI 이슈 해결

1. **128개 TS 에러**: main에 `fix/app-ci-ts-errors` 커밋이 있었는데 브랜치가 그 이전 base → `git rebase origin/main`
2. **PromotionGateResult 중복**: `import type` + `export type { } from` 둘 다 선언 → export 제거

## 상태

- PR #292 rebase 머지 완료
- main SHA: `cdefda4d`
- 모든 CI (App/Engine/Contract) 초록

**Why:** 다음 작업: 카피트레이딩 Phase 1 DB 스키마 또는 Layer A 40+차원 corpus (W-0145)
**How to apply:** main 기준 신규 브랜치로 시작
